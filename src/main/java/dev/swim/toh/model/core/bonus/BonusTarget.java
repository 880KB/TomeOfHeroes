package dev.swim.toh.model.core.bonus;

/**
 * Marker interface for anything a Bonus can apply to (e.g. AttributeName, SavingThrow, or a
 * dedicated target enum for a single value like max hit points).
 */
public interface BonusTarget {
}
